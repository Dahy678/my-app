version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:14  # O la imagen de tu preferencia
    steps:
      - checkout

      - run:
          name: Instalar SonarScanner
          command: |
            curl -sSLo sonar-scanner.zip https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.6.2.2472-linux.zip
            unzip sonar-scanner.zip
            sudo mv sonar-scanner-4.6.2.2472-linux /opt/sonar-scanner  # Usar sudo para mover a /opt
            echo 'export PATH=$PATH:/opt/sonar-scanner/bin' | sudo tee -a ~/.bashrc  # Usar sudo para modificar .bashrc
            source ~/.bashrc

      - run:
          name: Ejecutar SonarScanner
          command: |
            sonar-scanner \
              -Dsonar.projectKey=mi_proyecto \
              -Dsonar.host.url=http://localhost:9000 \
              -Dsonar.login=$SONAR_TOKEN

workflows:
  version: 2
  test_and_scan:
    jobs:
      - build
